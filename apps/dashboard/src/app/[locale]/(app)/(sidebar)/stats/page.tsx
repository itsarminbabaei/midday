import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Stats | Travelese",
};

export default async function Page() {

  return (
    <div className="mt-4">
      Stats
    </div>
  );
}
